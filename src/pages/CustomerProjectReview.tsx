  // │  page.ts │                │   protected by RLS    │         │
  // └──────────┘                └───────────────────────┘         │

  // Now open the first chore (create it if empty) and use the same keyboard to act.